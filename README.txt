Add this file to your Vercel project:

- api/owner-login.js

Required Vercel environment variables:
- OWNER_EMAIL=beentheredonetat@gmail.com
- OWNER_PASSWORD=Cucumber1211$

This endpoint safely checks owner credentials server-side so the password is never exposed in the browser.